#include "Pods-KeepLayout.xcconfig"
GCC_PREPROCESSOR_DEFINITIONS = COCOAPODS=1
HEADER_SEARCH_PATHS = "${PODS_ROOT}/BuildHeaders" "${PODS_ROOT}/BuildHeaders/KeepLayout" "${PODS_ROOT}/Headers" "${PODS_ROOT}/Headers/KeepLayout"
OTHER_LDFLAGS = -ObjC
PODS_ROOT = ${SRCROOT}